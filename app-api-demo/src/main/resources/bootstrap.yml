spring:
  application:
    name: app-api-demo
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
      name: app-api
      profile: ${config.profile:dev}
  rabbitmq:
    addresses: localhost
    port: 5672

encrypt:
  failOnError: false